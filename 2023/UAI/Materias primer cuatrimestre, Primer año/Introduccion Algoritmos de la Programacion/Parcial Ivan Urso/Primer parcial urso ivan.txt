#include <stdio.h>
#include <stdlib.h>
int main(int argc, char *argv[]) 
{
int CantCL=0, CantH=0, CantHT=0, FacT=0, TotalFac, TotalBon, TotalF, HoraIn, HoraFn, Dia=0, PrecioHora=0, F1, F2, F3, HoraT, FM, FMen, DescuentoT, Ganancia;
float Por1, Por2, Por3, Descuento=0, PrecioHoraT;

do
{
while (HoraIn != 24)
{
printf ("A que hora se conecto el usuario?\n");
scanf("%d", &HoraIn);
printf ("\nA que hora se desconecto el usuario?\n");
scanf("%d", &HoraFn);
printf ("\nSi el usuario se conecto en dia de semana escribir 1, si se conecto en fin de semana escribir 2\n");
scanf("%d", &Dia);
printf ("Cuantas horas se conecto el usuario?");
scanf("%d", &CantH);

if(Dia = 1)
	{
	PrecioHora = 50;
	}
if(Dia = 2)
	{
	PrecioHora = 65;	
	}
else
	{
	printf("\nError de dia\n");
	return 0;
	}
	if(HoraIn> 8 && HoraIn< 12)
	{
		Descuento = (PrecioHora*10)/100;
		F1++;
	}
	if(HoraIn >13 && HoraIn<20)
	{
		F2++;
	}
	if(HoraIn > 21 && HoraIn< 7)
	{
		Descuento = (PrecioHora*15)/100;
		F3++;
	}
	else
	{
		printf("\nError de Hora\n");
		return 0;
	}
	DescuentoT += Descuento;
	PrecioHoraT = PrecioHora * CantH;
	HoraT += CantH;
	CantCL += F1+F2+F3;
	
	if(F1>F2 && F2<F3)
	{
		FM = 1;
	}
	if(F1<F2 && F2>F3)
	{
		FM = 2;
	}
	if(F1<F2 && F2<F3)
	{
		FM = 3;
	}
		if(F1<F2 && F2>F3)
	{
		FMen = 1;
	}
	if(F1>F2 && F2<F3)
	{
		FMen = 2;
	}
	if(F1>F2 && F2>F3)
	{
		FMen = 3;
	}
	
	Ganancia = PrecioHoraT * Descuento;
	CantH += CantH;
	FacT += PrecioHoraT;
	}
	printf("\nCantidad total de cliemtes = %d\n", &CantCL);
	printf("\nCantidad de horas conectados = %d\n", &CantHT);
	printf("\nFranja que tuvo Mas conectados fue = %d\n", &FM);
	printf("\nFranja que tuvo Menos conectados fue = %d\n", &FMen);
	printf("\nEl total facturado fue de = %d\n", &FacT);
	printf("\nLa suma de los promedios es de = %d\n", &DescuentoT);
	printf("\nLa ganancia total fue de = %d\n", &Ganancia);
	return 0;
}